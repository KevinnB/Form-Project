export enum PermissionEntry {
    CanAccess = 0,
    CanEdit = 1,
    CanDelete = 2
}

export enum test {
    Popup = 0,
    Redirect = 1,
    Anonymous = 2,
    Password = 3,
    OAuthToken = 4,
    CustomToken = 5,
}